import {Link} from 'react-router-dom'; import {Footer} from '@/components/Footer/Footer'; import {Navbar} from '@/components/Navbar/Navbar'; import {articles} from '@/data/articles';
export function BlogPage(){return <><Navbar/><main id="contenu"><section className="page-hero"><div className="container"><p className="eyebrow">Blog & actualités</p><h1>Comprendre et faire grandir l’engagement</h1><p>Conseils, témoignages et dossiers sur le recrutement associatif, le mécénat de compétences et les initiatives solidaires.</p></div></section><section className="app-section"><div className="container"><div className="row g-4">{articles.map((article,index)=><div className={index===0?'col-12':'col-md-6'} key={article.slug}><article className={`card article-card p-4 h-100 ${index===0?'article-featured':''}`}><span className="article-icon"><i className={`bi ${article.icon}`}/></span><p className="eyebrow mb-2">{article.category}</p><h2 className={index===0?'h3':'h4'}>{article.title}</h2><p className="text-soft">{article.excerpt}</p><div className="article-meta"><span>{article.publishedAt}</span><span>{article.readingTime} de lecture</span></div><Link to={`/blog/${article.slug}`} className="stretched-link mt-3">Lire l’article <i className="bi bi-arrow-right"/></Link></article></div>)}</div></div></section></main><Footer/></>}