import { useState } from 'react';
import { Link } from 'react-router-dom';
import { Footer } from '@/components/Footer/Footer';
import { Navbar } from '@/components/Navbar/Navbar';
import { useSubmitMecenat } from '@/hooks/useMecenat';
import type { MecenatSubmission, SponsorshipMission } from '@/types/mecenat';

const initialForm: MecenatSubmission = { contact_email: '', company_name: '', company_legal_id: '', country_code: 'DZ', region: '', title: '', description: '', objectives: '', deliverables: '', required_profiles: '', man_days: 1, visibility: 'open', consent: false, website: '' };

export function MecenatPage() {
  const [form, setForm] = useState<MecenatSubmission>(initialForm);
  const [submittedMission, setSubmittedMission] = useState<SponsorshipMission | null>(null);
  const submitMecenat = useSubmitMecenat();
  const update = <K extends keyof MecenatSubmission>(key: K, value: MecenatSubmission[K]) => setForm((current) => ({ ...current, [key]: value }));

  function handleSubmit(event: React.FormEvent) { event.preventDefault(); submitMecenat.mutate(form, { onSuccess: setSubmittedMission }); }
  const trackingLink = submittedMission?.debug_verification_token ? `/mecenat/suivi?id=${submittedMission.tracking_uuid}&token=${encodeURIComponent(submittedMission.debug_verification_token)}` : '/mecenat/suivi';

  return <><Navbar /><main><section className="mecenat-hero"><div className="container"><p className="eyebrow mecenat-eyebrow">Mécénat de compétences</p><h1>Mettez vos compétences au service de l'intérêt général</h1><p className="mecenat-lead">Proposez une mission à des organisations sans créer de compte.</p><a href="#depot" className="btn btn-accent btn-lg">Déposer une mission</a></div></section><section id="depot" className="app-section section-tinted"><div className="container"><div className="section-head"><p className="eyebrow">Sans compte à créer</p><h2>Déposer une mission de mécénat</h2><p>Les champs sont envoyés directement au service Django.</p></div><div className="row g-4"><div className="col-lg-8">{submittedMission ? <div className="notice notice-success"><div className="notice-body"><h4>Mission déposée avec succès</h4><p>Un suivi est associé à l'adresse {submittedMission.contact_email}.</p><Link to={trackingLink} className="btn btn-primary mt-3">Suivre et vérifier la mission</Link></div></div> : <form onSubmit={handleSubmit}>{submitMecenat.isError && <div className="alert alert-danger">{submitMecenat.error instanceof Error ? submitMecenat.error.message : 'Envoi impossible.'}</div>}<div className="form-block"><h3>Référent et entreprise</h3><div className="row g-3"><div className="col-sm-6"><label htmlFor="contact_email" className="form-label">E-mail professionnel *</label><input id="contact_email" type="email" className="form-control" required value={form.contact_email} onChange={(event) => update('contact_email', event.target.value)} /></div><div className="col-sm-6"><label htmlFor="company_name" className="form-label">Raison sociale *</label><input id="company_name" className="form-control" required value={form.company_name} onChange={(event) => update('company_name', event.target.value)} /></div><div className="col-sm-6"><label htmlFor="company_legal_id" className="form-label">Identifiant légal</label><input id="company_legal_id" className="form-control" value={form.company_legal_id} onChange={(event) => update('company_legal_id', event.target.value)} /></div><div className="col-sm-3"><label htmlFor="country_code" className="form-label">Pays</label><select id="country_code" className="form-select" value={form.country_code} onChange={(event) => update('country_code', event.target.value)}><option value="DZ">Algérie</option><option value="FR">France</option><option value="MA">Maroc</option></select></div><div className="col-sm-3"><label htmlFor="region" className="form-label">Ville / région</label><input id="region" className="form-control" value={form.region} onChange={(event) => update('region', event.target.value)} /></div></div></div><div className="form-block"><h3>La mission</h3><div className="mb-3"><label htmlFor="title" className="form-label">Intitulé *</label><input id="title" className="form-control" required value={form.title} onChange={(event) => update('title', event.target.value)} /></div><div className="mb-3"><label htmlFor="description" className="form-label">Description</label><textarea id="description" className="form-control" rows={3} value={form.description} onChange={(event) => update('description', event.target.value)} /></div><div className="row g-3"><div className="col-md-4"><label htmlFor="man_days" className="form-label">Jours-homme *</label><input id="man_days" type="number" min="1" className="form-control" required value={form.man_days} onChange={(event) => update('man_days', Number(event.target.value))} /></div><div className="col-md-8"><label htmlFor="visibility" className="form-label">Visibilité *</label><select id="visibility" className="form-select" value={form.visibility} onChange={(event) => update('visibility', event.target.value as MecenatSubmission['visibility'])}><option value="open">Ouverte</option><option value="verified_organizations">ONG vérifiées uniquement</option></select></div></div></div><div className="form-check mb-3"><input id="consent" type="checkbox" className="form-check-input" checked={form.consent} onChange={(event) => update('consent', event.target.checked)} required /><label htmlFor="consent" className="form-check-label">J'accepte que ces informations soient traitées pour cette demande. *</label></div><div className="form-actions"><button type="submit" className="btn btn-accent" disabled={submitMecenat.isPending}>{submitMecenat.isPending ? 'Envoi…' : 'Déposer la mission'}</button><Link to="/mecenat/suivi" className="btn btn-subtle">Suivre une mission</Link><Link to="/" className="btn btn-subtle">Annuler</Link></div></form>}</div></div></div></section></main><Footer /></>;
}